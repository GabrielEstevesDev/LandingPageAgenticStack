services:
  landing:
    build: .
    container_name: agentic-landing
    restart: unless-stopped
    networks:
      - agentaiforpromotion_default
    expose:
      - "3000"

networks:
  agentaiforpromotion_default:
    external: true
